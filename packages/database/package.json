{
  "private"           : true,
  "name"              : "@obvia/database",
  "description"       : "Reusable database presets for consistent schema design, type-safe queries, and scalable database workflows across projects.",
  "keywords"          : ["obvia", "prisma", "postgresql", "type-safe", "orm", "schema", "preset", "database-tools", "database", "workflows"],
  "license"           : "MIT",
  "homepage"          : "https://obvia.studio/",
  "version"           : "0.0.1",

  "author"            : {
    "name"  : "Selçuk Çukur",
    "email" : "hello@selcukcukur.me",
    "url"   : "https://selcukcukur.me"
  },

  "bugs"              : {
    "email" : "security@obvia.studio",
    "url"   : "https://github.com/obvialabs/obvia/issues"
  },

  "scripts"           : {
    "generate"  : "bun prisma generate",
    "push"      : "bun prisma db push",
    "studio"    : "bun prisma studio",
    "format"    : "bun prisma format",
    "sync"      : "bun prisma generate && bunx prisma db push",
    "test"      : "bun test --bail --no-file-parallelism"
  },

  "main"              : "./dist/index.mjs",
  "module"            : "./dist/index.mjs",
  "types"             : "./dist/index.d.ts",

  "dependencies"      : {
    "@obvia/utilities"              : "^0.0.4",
    "@prisma/adapter-pg"            : "catalog:db",
    "@prisma/client"                : "catalog:db",
    "@prisma/extension-accelerate"  : "catalog:db",
    "@prisma/client-runtime-utils"  : "catalog:db",
    "@prisma/config"                : "catalog:db"
  },
  "devDependencies"   : {
    "@types/node"                   : "catalog:type",
    "@types/pg"                     : "^8.18.0",
    "prisma"                        : "catalog:db",
    "typescript"                    : "catalog:core"
  },

  "exports"           : {
    "."             : "./src/index.ts",
    "./browser"     : "./src/client/index.ts",
    "./client"      : "./src/client/client.ts",
    "./edge"        : "./src/client/edge.ts"
  },

  "publishConfig"     : {
    "access"    : "public"
  },
  "publisher"         : {
    "type"      : "github",
    "user"      : "obvialabs",
    "repo"      : "obvia"
  },
  "repository"        : {
    "type"      : "git",
    "url"       : "git+https://github.com/obvialabs/obvia.git",
    "directory" : "packages/database"
  }
}
